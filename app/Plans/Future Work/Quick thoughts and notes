when they upload a file, do i want to get and use this meta data? File metadata database schema with full CRUD operations

on conversation storage -  Auto-title generation from first message
    should it pull out key things
    organize it by topics
    measure stuff to predict and undersatnd the user more?


☐ Create model selector with logic for choosing GPT-4 vs GPT-4 Vision
    need to make this so i can change the models in the future, for redundnacy and cost